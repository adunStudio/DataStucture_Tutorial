#include <stdio.h>
#include <stdlib.h>

typedef struct _node
{
    int data;             // 데이터를 담을 공간
    struct _node* next;   // 연결

} Node;

int main()
{
    Node* head = null;    // 리스트의 머리를 가리키는 포인터 변수
    Node* tail = null;    // 리스트의 꼬리를 가리키는 포인터 변수
    Node* cur  = null;    // 저장된 데이터의 조회에 사용되는 포인터 변수

    int numOfData;
    int(*comp)

    return 0;
}